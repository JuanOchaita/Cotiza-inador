# Docker:
docker compose down
docker compose up -d
docker cp schema.sql postgres_db:/schema.sql
docker exec -i postgres_db psql -U myuser -d mydb < schema.sql
docker ps

docker cp "PATH/schema.sql" postgres_db:/schema.sql

# Vulkan Set
docker cp card_information_en.csv postgres_db:/tmp/cards.csv
docker exec -i postgres_db psql -U myuser -d mydb < bulk_insert_from_csv.sql

# Entrar a posgresql Terminal
docker exec -it postgres_db psql -U myuser -d mydb

# Salir de posgresql Terminal
\q

# Ver toda la info de las tables:

SELECT * FROM "card";
SELECT * FROM "card_condition";
SELECT * FROM "card_in_collection";
SELECT * FROM "collection";
SELECT * FROM "price";
SELECT * FROM "user";
